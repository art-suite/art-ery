import &ArtStandardLib, &ArtCommunicationStatus, &Art/Ery

class ArtEryInfoHandler extends &ExpressPromiseHandlerBase

  @getter
    handleUrlRegex: -> @_exactDefaultHandlerRegex ||= /// ^ \/ #{config.apiRoot} \/? $

  handleApiRequest: (request, requestData) ->
    Art.Ery.pipeline.json.rest.api:
      object pipeline from pipelines with pipeline.getApiReport {} server
